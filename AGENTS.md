# Agent Instructions (Nexus)

## "Land the plane" (definition)
Before handing off, you must:
1) `bun test` is green
2) `bd sync`
3) `git status` is clean
4) commit any handoff state changes (esp. `AGENTS.md`)
5) if a remote exists: `git push` (don't strand work locally)

This repo uses **bd** (beads) for tasks.

## Repo Policy
- **Bun + TypeScript** for implementation + tests.
- **No markdown sprawl**: record decisions inside the *relevant bd issue* (update the issue description). No DECISIONS.md, no extra docs.
- Keep this single **AGENTS.md** as the only persistent "handoff/state" file.

## Quick Reference

```bash
bd ready
bd show <id>
bd update <id> --status in_progress
bd close <id>

bun test
```

## Current State (handoff)
- Repo: `/home/imrane/src/tries/2026-02-14-nexus`
- Stack: Bun + TypeScript, TDD via `bun test`
- Tracking: `bd` in `.beads/` (decisions live in bd issue descriptions)
- **Status**: ✅ MVP complete (all P0 tasks closed)

### Implemented (merged locally)
- **Core parsing/validation**:
  - canonical ids: `src/canonicalId.ts`
  - pack schema: `src/packSchema.ts` (includes `enable` field)
  - enable parsing + missing-enabled validation: `src/enable.ts`, `src/enableValidation.ts`
  - collision detection: `src/collision.ts`
  - canonical graph compiler: `src/graph.ts`
- **Emitters** (all working):
  - Claude: `src/emitters/claude.ts` → `.claude/skills/*` + `.nexus-managed`
  - MCP: `src/emitters/mcp.ts` → `.mcp.json` with `__generated_by: nexus`
  - Codex: `src/emitters/codex.ts` → `.codex/config.toml` header `# generated by nexus`
  - OpenCode: `src/emitters/opencode.ts` → `.opencode/commands/*` + `.nexus-managed`
- **Cleanup + state**:
  - owned-only cleanup: `src/cleanup.ts`
  - state hash + state file: `src/state.ts`, `src/stateFile.ts` (stores `.claude/.nexus-state`)
- **CLI + activation**:
  - `src/index.ts` - full CLI that loads pack, compiles graph, emits outputs
  - `flake.nix` - Nix devShell with automatic activation via `shellHook`
  - `nix flake check` - runs `bun test` in isolated build
- **Tests** (36 passing):
  - Unit tests for all components
  - Golden test: `test/goldenCorePack.test.ts`
  - Integration tests:
    - `test/integrationMissingEnabled.test.ts` - validates error on missing enabled items
    - `test/integrationCollision.test.ts` - validates error on id collisions
    - `test/integrationStateNoop.test.ts` - validates no-op behavior when state unchanged

### Closed bd issues (MVP complete)
- ✅ `2026-02-14-nexus-5bn` Nix flake + devShell + check
- ✅ `2026-02-14-nexus-3cr` Activation via shellHook
- ✅ `2026-02-14-nexus-25k` Integration: missing enabled item test
- ✅ `2026-02-14-nexus-2ww` Integration: collision test
- ✅ `2026-02-14-nexus-vjy` Integration: state hash no-op test
- ✅ `2026-02-14-nexus-8al` Epic: MVP Nexus core

### Open bd issues (post-MVP)
P1:
- `2026-02-14-nexus-15m` PRD doc (optional per "no markdown sprawl")
P2:
- `2026-02-14-nexus-sa5` rename git branch master→main

### How to use
```bash
cd ~/src/tries/2026-02-14-nexus/fixtures/packs/core
nix develop  # Auto-runs nexus, emits .claude/skills etc.
```

Or run manually:
```bash
cd <pack-root>
bun ~/src/tries/2026-02-14-nexus/src/index.ts
```

### Notes
- All tests pass: `bun test` (36 passing)
- `nix flake check` passes
- Git identity set locally in repo
- No git remote configured yet (local commits only)

## Landing the Plane
1. Run tests: `bun test`
2. `bd sync`
3. Commit with issue ids in the message (e.g. `feat: emitter claude (2026-02-14-nexus-8eh)`)
