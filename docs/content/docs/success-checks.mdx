---
title: Success Checks
description: How to confirm your first run worked.
---

You should see all of these:

- `replix add` says `added` (or `already present`).
- `replix` finishes without errors.
- `replix check` reports outputs are in sync.
- `.replix/packs.json` includes your pack source.
